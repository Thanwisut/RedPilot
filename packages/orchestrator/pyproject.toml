[project]
name = "redpilot-orchestrator"
version = "0.1.0"
description = "REDPILOT orchestration engine — Master Agent, Task Manager, scheduling"
requires-python = ">=3.12"
dependencies = ["redpilot-core", "redpilot-tools"]

[tool.uv.sources]
redpilot-core = { workspace = true }
