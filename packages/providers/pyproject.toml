[project]
name = "redpilot-providers"
version = "0.1.0"
description = "REDPILOT LLM provider abstraction layer"
requires-python = ">=3.12"
dependencies = ["redpilot-core"]

[tool.uv.sources]
redpilot-core = { workspace = true }
