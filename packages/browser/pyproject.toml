[project]
name = "redpilot-browser"
version = "0.1.0"
description = "REDPILOT browser automation — Playwright MCP integration"
requires-python = ">=3.12"
dependencies = ["redpilot-core"]

[tool.uv.sources]
redpilot-core = { workspace = true }
